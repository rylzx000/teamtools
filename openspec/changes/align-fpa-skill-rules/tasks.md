## 1. 契约与提示词

- [x] 1.1 更新提示词模板，明确冻结清单唯一依据、系统字典原样命名、数据功能支撑过程和 `AI评估.md` 内容要求。
- [x] 1.2 更新 AI 输出 JSON Schema，新增 `linked_process_ids` 和 `linked_data_ids` 字段。

## 2. 校验与生成脚本

- [x] 2.1 更新 AI 结果校验脚本，校验新增追溯字段格式、悬空引用、数据功能支撑过程说明和系统字典必填规则。
- [x] 2.2 更新 Excel 生成脚本，过程 JSON 保留新增追溯字段，不写入 Excel 明细列，并移除业务充分性阻断提示。
- [x] 2.3 检查后端 Excel payload 透传，确保新增追溯字段不丢失。

## 3. 测试与文档

- [x] 3.1 补充或更新后端单元测试，覆盖数据功能追溯校验、系统字典校验、过程 JSON 保留字段、质量门禁降级和 happy path。
- [x] 3.2 同步 FPA 模块文档，清理旧 `items` 契约并说明普通用户/管理员可见性边界。
- [x] 3.3 同步 OpenSpec 规格并运行 strict 校验、后端单测、编码检查和 diff 检查。

## 4. 审查后收口

- [x] 4.1 基于最新 `origin/main` 重新回放改动，并保留生产系统范围、登录标题、密码和初始化用户逻辑。
- [x] 4.2 收紧 `ILF/EIF` 缺少 `linked_process_ids` 时的 `review_notes` 绑定校验，并补充正反向单测。
- [x] 4.3 修正 `AI评估.md` 提示词示例和文档/OpenSpec 口径。
