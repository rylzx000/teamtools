## 1. 契约与资源对齐

- [ ] 1.1 对照新版 `fpa-skill` 梳理 TeamTools FPA profile 的提示词、schema、mapping、模板和样例差异
- [ ] 1.2 更新 FPA 提示词模板，使 AI 一次响应包含 `AI评估.md` 内容和 `AI结构化结果.json` 内容
- [ ] 1.3 更新 `result.schema.json`，增加 `stable_id`、`fact_ids`、`route_ids`、`system_scene_ids`、拆分/合并依据和冻结清单结构，并校验重复编号、悬空引用和缺失追溯关系
- [ ] 1.4 更新系统资料读取规则，优先读取 `08-FPA场景拆分字典.md`，再读取 `teamtools-system-brief.md`
- [ ] 1.5 更新系统资料简包规则和样例，确认 `claimcar`、`claimoth`、`onlineclaim`、`clqp` 的资料目录命名和系统编码一致
- [ ] 1.6 明确 `规模计数时机` 和 `完整性级别` 的下拉选项、系数前缀和默认值，其他项目特征使用模板默认值

## 2. 后端任务与产物链路

- [ ] 2.1 更新 AI 请求包生成逻辑，确保只把模型需要的正文发送给前端调用模型
- [ ] 2.2 更新 AI 结果接收逻辑，分别提取并保存 `AI评估.md` 和 `AI结构化结果.json`
- [ ] 2.3 更新 AI 结构化结果校验逻辑，拒绝缺少冻结清单、枚举非法、包含计算字段或追溯关系不完整的输出
- [ ] 2.4 更新任务产物保存逻辑，普通用户可见 `AI评估.md` 和 Excel，后台保留 `AI结构化结果.json` 和 `FPA生成过程.json`
- [ ] 2.5 更新普通用户和管理员的文件可见性，确保结构化 JSON、过程 JSON、请求包、payload、日志不在普通用户文件列表或下载入口展示
- [ ] 2.6 确保脚本 payload 默认不登记为普通产物；管理员排查临时保留时标记为 admin-only/internal debug/runtime temp

## 3. Excel 生成链路

- [ ] 3.1 更新 Excel 脚本输入 payload 适配冻结功能点清单，并保留 `stable_id` 到过程 JSON
- [ ] 3.2 调整 Excel 脚本校验，只保留模板、字段、备注、公式、目标命中和结构提醒，不做业务补点
- [ ] 3.3 确认模板复制、sheet 顺序、项目特征、公式、样式、动态明细行和合计行与模板保持一致
- [ ] 3.4 确认 Excel 明细、`AI评估.md` 摘要和过程 JSON 使用同一组冻结功能点

## 4. 页面交互

- [ ] 4.1 更新提交页，增加 `规模计数时机` 和 `完整性级别` 下拉框，选项文字带系数前缀
- [ ] 4.2 更新任务详情页流程展示为 `提交需求`、`AI评估中`、`生成结果中`、`评估完成/评估失败`
- [ ] 4.3 更新完成页，只展示结果摘要、`AI评估.md` 预览和 Excel 下载入口

## 5. 验证与样例

- [ ] 5.1 补充新版 AI 响应样例、结构化结果样例和校验失败样例
- [ ] 5.2 补充 Excel payload、过程 JSON 和生成 Excel 的回归样例
- [ ] 5.3 使用同一需求跑通端到端流程，验证任务状态、产物权限、Excel 下载和页面预览
- [ ] 5.4 验证 30 条和 50 条功能点生成同一个 Excel，覆盖动态扩行、空白行删除、公式范围、样式、数据验证和合计行
- [ ] 5.5 验证冻结清单、`AI评估.md`、Excel 明细和过程 JSON 的数量、顺序、类型、`stable_id` 完全一致
- [ ] 5.6 验证命中 `08-FPA场景拆分字典.md` 时，Excel 一级模块、二级模块和功能点计数项名称必须沿用字典值
- [ ] 5.7 验证 payload、结构化 JSON、过程 JSON 不作为普通产物、普通用户不可下载
- [ ] 5.8 验证目标人天只影响校准说明和参数，不改变冻结清单数量或拆分边界
- [ ] 5.9 运行编码检查和相关前后端/脚本测试，记录无法运行的检查原因
