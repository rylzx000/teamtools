## 1. OpenSpec / Comet 初始化

- [x] 1.1 确认 `apply-open-design-fpa-frontend` change 使用新的活跃目录，不修改已归档 `frontend-page-style-adjustment`
- [x] 1.2 补齐 proposal、design、tasks 和相关 delta spec，并通过 open 阶段校验

## 2. 后端表单配置与产物边界

- [x] 2.1 在后端最小补充 `GET /api/fpa/form-config`，返回系统、规模计数时机、完整性级别和默认值
- [x] 2.2 确保表单配置和系统列表不暴露 `knowledge_dir` 或服务器真实资料路径
- [x] 2.3 预留普通用户可见的 AI Markdown 预览字段或文件角色，避免普通用户看到后台排查产物
- [x] 2.4 补充或调整后端相关测试，覆盖表单配置和普通用户产物边界

## 3. 前端平台壳与路由入口

- [x] 3.1 登录成功、访问 `/` 或 `/modules` 时默认进入 `/fpa/tasks`
- [x] 3.2 首版隐藏首页模块卡片和左侧模块栏，只保留顶部模块标识、用户、角色、退出登录和 FPA 页签
- [x] 3.3 保留任务列表、提交评估、查看详情三个页签和当前页选中态

## 4. 任务列表页

- [x] 4.1 按 Open Design 风格重做列表页布局，压缩字体和间距并避免常规桌面横向滚动
- [x] 4.2 默认展示任务名称、系统、状态、提交时间、完成时间、目标人天、结果中值、命中目标和操作
- [x] 4.3 列表操作收敛为查看；已完成且可下载时额外展示下载
- [x] 4.4 管理员提交人字段弱化展示，不挤压核心字段

## 5. 提交评估页

- [x] 5.1 使用 `GET /api/fpa/form-config` 加载系统、规模计数时机和完整性级别
- [x] 5.2 添加带系数前缀的规模计数时机和完整性级别下拉展示，提交真实值不拼接系数
- [x] 5.3 保留目标人天、需求名称、Markdown 粘贴、`.md` 文件上传、API Key 本地输入和记住到本机浏览器
- [x] 5.4 提交任务不上传 API Key，成功后跳转任务详情页

## 6. 任务详情页

- [x] 6.1 按 `waiting_ai_call`、`validating_result`、`generating_result`、`completed`、`failed`、`cancelled/canceled` 渲染主内容
- [x] 6.2 将细粒度状态映射为提交需求、AI评估中、生成结果中、评估完成、评估失败和已取消
- [x] 6.3 等待 AI 调用态展示任务摘要、API Key 状态、调用模型入口和系统轻校验提示
- [x] 6.4 完成态展示结果中值、目标命中、质量提示、Excel 下载和 AI Markdown 预览/复制
- [x] 6.5 失败态和取消态展示脱敏原因、建议操作、返回列表、重新调用或复制重建入口
- [x] 6.6 管理员排查信息默认折叠，普通用户不展示完整后台 JSON、payload、日志、原始模型响应或敏感路径

## 7. 验证

- [x] 7.1 运行后端相关测试，至少覆盖 `form-config` 和 FPA MVP 主链路未回归
- [x] 7.2 运行前端构建或项目已有类型检查
- [x] 7.3 运行 `.\scripts\check-encoding.ps1`
- [x] 7.4 运行 `openspec validate --all --strict`
- [x] 7.5 如环境允许，启动本地服务并用浏览器冒烟检查任务列表、提交评估和任务详情三页

<!-- review note: standard 自动代码审查派发因当前工具参数解析失败未完成；已执行本地 diff 自审和 git diff --check，未发现 Critical/Important 问题。 -->

## 8. OpenDesign 差异修正

- [x] 8.1 详情页顶部操作改为复制任务 ID 和返回任务列表，取消任务与复制并重新生成收入折叠操作区
- [x] 8.2 详情页信息层级调整为状态进度加任务摘要两栏，结果查看收敛为单栏区域
- [x] 8.3 任务列表标题、工具条和管理员字段开关对齐 OpenDesign
